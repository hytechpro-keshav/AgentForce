# Project Memory

- This repo is the canonical monorepo for Salesforce Agentforce metadata plus the external AI platform.
- Monorepo platform paths: `apps/ai-api`, `apps/react-chat-window`, `apps/openwebui`, and `packages/*` for shared contracts or platform modules.
- The NestJS/Railway/OpenAI/LangChain/Pinecone/Open WebUI plan now lives in this repo; keep runtime ownership and security boundaries separate.
- First milestone: Agentforce -> Apex -> Named Credential -> Railway NestJS -> structured response -> Agentforce.
- Phase 1 completion on 2026-05-11: Railway ai-api was deployed, Salesforce credential metadata was deployed, `Customer_Self_Service_Agent` was bound to `Check_AI_API_Health`, runtime proof was captured with preview plus Apex logs, and local Apex tests passed with org-wide coverage back at 76%. An earlier `Agentforce_Service_Agent` proof was superseded and its temporary health topic was removed.
- Phase 2 backend foundation completion on 2026-05-11: `apps/ai-api` now has LLM provider contracts, OpenAI and OpenAI-compatible adapters, `ModelRouter` fallback, JWT auth, DTO validation, no-op-safe `gen_ai.*` telemetry, `/chat/message`, `/agent/support/triage-case`, `/v1/models`, and `/v1/chat/completions`. Validation passed 38 unit tests, 14 e2e tests, ai-api typecheck, and ai-api build.
- Phase 2 through-Agentforce proof completion on 2026-05-11: `Customer_Self_Service_Agent` invoked `Triage Support Case`, Apex `AgentforceAiApiSupportTriage` called `Agentforce_AI_API_Phase2/agent/support/triage-case`, the first live Railway proof ran on deployment `9b17ac02-4ddc-4f28-9c3f-b7b101d173b6`, masking was hardened on deployment `83c45bdb-161c-4441-8d78-9f4c27a66e2c`, and the current closeout deployment `6522fb47-ad6c-48b4-a541-3bcb1f795226` emitted cost-reference telemetry for the live `gpt-4o-mini` request. Core Salesforce deploy `0Afg5000007rC5RCAU`; final planner deploy `0Afg5000007rD6NCAU`.
- Phase 2 credential lesson: store `Agentforce_AI_API_Phase2` custom credential values through `/services/data/v66.0/named-credentials/credential`, not `/connect/named-credentials/credential`. Do not add unsupported `AuthParameter` metadata for Custom External Credentials; the encrypted value can exist only in credential storage.
- Phase 2 planner lesson: Agentforce planner-local actions need local input/output schema files in the planner bundle even when the global `genAiFunction` has schemas. Missing planner-local schemas caused the action to appear unavailable at runtime.
- Phase 2 model lesson: `gpt-4.1-mini` returned `model_not_found` for the current OpenAI project; current Railway deployment uses `OPENAI_DEFAULT_MODEL=gpt-4o-mini`.
- Phase 2 masking hardening on 2026-05-11: Apex now masks common identifiers before `Agentforce_AI_API_Phase2` callouts, NestJS `ModelRouter` masks every `LlmChatRequest` before provider dispatch, and planner instructions tell Customer Self Service not to echo raw identifiers. Railway deploy `83c45bdb-161c-4441-8d78-9f4c27a66e2c`; Salesforce masking deploy `0Afg5000007rFplCAE`; preview session `019e1788-a7c4-7d57-9012-a7eb1984c9c2`; Apex log `07Lg5000006wxkDEAQ`; Railway request `sf-triage-1778511330722-0`.
- Phase 3 case-analysis live proof on 2026-05-11: Railway deployment `9ed18b87-bab8-4194-8f40-4b985bfd439f`; Salesforce core deploy `0Afg5000007rwzVCAQ`; planner deploy `0Afg5000007rrxVCAQ`; `Agentforce_AI_API_Phase2` credential revision `2` now carries combined scopes `agentforce:support-triage agentforce:case-analysis`. Preview session `019e17f5-da92-7985-a175-dcda32fd71ee`; Apex log `07Lg5000006ww1qEAA`; Railway HTTP request `ps5HMH7PRgOXaVCG-8Y8hA`; telemetry request `sf-case-analysis-1778518466738-0`; tokens `169/47/216`; estimated cost `0.00005355` USD. Proof doc: `docs/testing/phase3-agentforce-case-analysis-proof.md`.
- Phase 4 Knowledge RAG implementation on 2026-05-11/12: `apps/ai-api` now has `EmbeddingProvider`, OpenAI embeddings, deterministic local embeddings, `VectorStore`, Qdrant and Pinecone adapters, memory test store, RAG ingestion/search/answer services, `/rag/ingest`, `/rag/search`, `/agent/knowledge/answer`, sample corpus, safe RAG telemetry, `AgentforceAiApiKnowledgeRag`, `Answer_Knowledge_RAG`, planner topic `AI_API_Knowledge_RAG`, evals, and UAT/proof docs. The no-Pinecone proof path uses Railway-hosted Qdrant to avoid initial Pinecone spend.
- Phase 4 Qdrant deployment on 2026-05-12: Railway service `qdrant` with persistent `/qdrant/storage` volume and API-key auth was created, and `ai-api` deployment `4ace9b63-cb26-4d2d-b76e-fdc6c63567b0` started successfully with Qdrant RAG enabled. Direct proof is blocked because the current OpenAI project lists no embedding models and returns `model_not_found` for `text-embedding-3-small`, `text-embedding-3-large`, and `text-embedding-ada-002`; RAG was parked false pending embedding access.
- Phase 4 live Qdrant proof completed on 2026-05-12 after `text-embedding-3-small` became available: Railway deployment `be197048-ba39-4b5f-9e5e-ec6d954bee99` served successful sample ingestion plus grounded direct answer proof against Qdrant. Live tuning showed the Qdrant/OpenAI path needed `RAG_SCORE_THRESHOLD=0.68` instead of `0.72`; at `0.68` the supported troubleshooting prompt grounded to `kb-troubleshoot-intermittent-service-v1` while the unsupported executive-compensation prompt still returned `NO_SOURCE`.
- Phase 4 final closeout on 2026-05-12: backend hardening deployment `7c310667-493f-4f69-a88e-0f930034b55f` added RAG route rate limiting, zero-vector rejection, and Qdrant collection-race tolerance. Salesforce core deploy `0Afg5000007uddmCAA`, planner deploy `0Afg5000007upftCAA`, credential `Agentforce_AI_API_Phase2` value `0pwg5000000JRNtAAO` revision `3`, and Agentforce preview session `019e1b14-b15b-7eed-b6f7-b23ccc7bbcb4` proved source-cited Knowledge RAG via retrieval `rag-65b4a589-7084-4168-b8f4-c6302ed5ad4e`; later manual stakeholder preview returned retrieval `rag-a2334fff-68ba-4481-b634-c6bdc47175b2`. Unsupported knowledge returned no-source.
- Phase 4 RAG auth repair on 2026-05-13: Agentforce returned `AUTH_ERROR`/HTTP `401` for `Answer_Knowledge_RAG` while health and direct Railway RAG still passed, indicating the stored Salesforce JWT was expired/rejected. Refreshed `Agentforce_AI_API_Phase2` encrypted value `0pwg5000000JRNtAAO` to revision `5` with Agentforce scopes only; direct Apex smoke then returned `ANSWERED`/HTTP `201` with source `kb-troubleshoot-intermittent-service-v1` and retrieval `rag-c4415f28-ba02-4df7-b7bd-ecbffaca4bfb`. Programmatic preview session `019e2148-7286-70c0-90ce-db8fa00ee913` returned the approved troubleshooting answer after confirmation.
- Phase 4 durable auth deployment on 2026-05-13: AI API deployment `60c1bca1-816f-49b4-970e-9e889b213bcf` added hash-validated opaque Agentforce service bearer auth. Railway stores only `AI_API_AGENTFORCE_BEARER_TOKEN_SHA256`; Salesforce encrypted credential `0pwg5000000JRNtAAO` is revision `6` with the raw opaque token. Re-ingested sample corpus now returns the YouTube source URL. Direct opaque request retrieval `rag-8746fbf0-1728-47e3-87e3-1e7ad9e4b130`, Apex retrieval `rag-d59c1bea-e328-496a-93b2-410bf865e672`, and preview session `019e216b-9378-7291-a2b8-2bf537694fbc` all passed; invalid bearer returned HTTP `401`.
- Phase 5 Open WebUI on 2026-05-12: AI API deployment `9832cea7-a2a7-4043-9ccc-0919f69126c4` and Open WebUI deployment `7ae31cc8-eef3-4418-9616-95634a839ced` are live on Railway. Open WebUI URL is `https://openwebui-production-0f51.up.railway.app`, volume `openwebui-volume` (`6a9e1206-c601-4d31-a6fc-ce9308ee8385`) is mounted at `/app/backend/data`, `/v1/models` is constrained to only `knowledge-rag`, and live gateway smoke passed for `knowledge-rag`. First-admin setup, logged-in browser chat proof, security sign-off, and retention owner assignment remain manual steps.
- Phase 8 Services Org Intelligence completion on 2026-05-14: Railway `POST /agent/services/project-health` is live behind `Agentforce_AI_API_Phase2`; Salesforce added `AgentforceAiApiProjectHealth`, `AgentforcePsaProjectDirectory`, `Summarize_Project_Health_Brief`, `List_PSA_Projects`, and the dedicated Employee Agent `Services_Org_Intelligence_Showcase_Agent_new`. Multi-org opaque bearer isolation was implemented with `AI_API_AGENTFORCE_BEARERS_JSON`. Final UX polish deploys `0Afam00002UiH9JCAV` and `0Afam00002UiHHNCA3` produced a readable Project Directory plus a Project Health Brief with `Overall`, `Status snapshot`, `Why this result`, `Signals reviewed`, and a short action plan. Canonical continuity docs: `docs/agents/services-org-intelligence.md`, `docs/context/phase8-session-summary.md`, `docs/testing/phase8-services-org-intelligence-proof.md`, `docs/deployment/railway-ai-api-phase8.md`, and `specs/services-org-intelligence-agent.yaml`.
- Exact user-run masking verification on 2026-05-11: the user-supplied published-agent conversation matched Apex log `07Lg5000006wzFlEAI` and Railway request `sf-triage-1778511959312-0`; searches of Salesforce and Railway logs for the raw sample values returned no matches.
- Phase 2 telemetry closeout on 2026-05-11: latest preview session `019e179d-90ed-74a4-b598-5b03887373ec`, TraceFlag `7tfg5000003nWufAAE`, Apex log `07Lg5000006x1xWEAQ`, Railway HTTP request `X2iB2swyTKm70MfSvEOTxA`, and telemetry request `sf-triage-1778512685428-0` showed `gen_ai.usage.input_tokens=177`, `gen_ai.usage.output_tokens=45`, `gen_ai.usage.total_tokens=222`, and `gen_ai.usage.total_cost_usd_estimate=0.00005355` with static pricing source `static_openai_reference_2026_05`.
- Open WebUI is production internal chat. It calls the NestJS OpenAI-compatible gateway, not OpenAI directly.
- React chat is customer-facing and must use customer-safe policy, identity/session rules, rate limits, and escalation guardrails.
- React chat may be embedded in Experience Cloud or another Salesforce-hosted page shell, but the React codebase and backend lifecycle remain in `apps/react-chat-window`.
- Agent services call `ModelRouter`; `ModelRouter` calls provider adapters.
- OpenAI is production v1. Anthropic, Azure OpenAI, Gemini, and OpenAI-compatible self-hosted providers remain extension paths.
- Qdrant is the no-Pinecone production proof vector DB; Pinecone remains supported. Keep vector DB access behind an interface.
- Agentforce behavior changes need eval prompts or Testing Center cases.
- Production go-live requires UAT, security review, release approval, observability, rollback, and readiness gates.
- Built-in `Service Customer Verification` resolves a matching Salesforce `User` before a matching `Contact` when both share the same email. Use a non-colliding `Contact` email for customer self-service OTP testing unless login-user verification is intentional.
- OTP operational guidance for this org: tell customers to check spam or junk, wait about 1 to 2 minutes, resend only when needed, and use only the newest code because resends invalidate older OTPs.
- Earlier direct Salesforce mail tests and verification-flow logs proved the Agentforce OTP path can queue mail successfully. Current missing OTPs must also be checked against org email limits before assuming mailbox filtering.
- Manual OTP sends can appear to contradict Agentforce sends if they happened earlier or used a different recipient/execution user. On 2026-05-07, manual Flow send at 06:40 UTC succeeded for `keshavchaudhary131@gmail.com`; later Agentforce runtime send at 08:03 UTC failed for the plus-alias Contact because `SingleEmail` was exhausted.
- On 2026-05-07, `CustomerSelfServiceEscalateRequest` was hardened so escalation by customer-provided case number requires the verified account context. Targeted deploy `0Afg5000007dTW0CAM` passed 8/8 `CustomerSelfServiceActionsTest` tests.
- `Work.com Only User` licenses cannot receive the `Customer_Self_Service_Agent` permission set because that set requires Account, Contact, and Case access.
- Dev-only fallback: the latest OTP can be read from the admin debug log near the `GenerateVerificationCode` flow action for `SvcCopilotTmpl__SendVerificationCode`. Do not treat this as a production support pattern.
- Active `GenAiPlannerBundle` deployment requires the supported lifecycle sequence: `sf agent deactivate --api-name Customer_Self_Service_Agent --target-org AgentForce`, deploy, then `sf agent activate --api-name Customer_Self_Service_Agent --target-org AgentForce`, followed by `sf agent preview ...` validation.
- On 2026-05-07, the org hit the Developer Edition external `SingleEmail` limit: `/limits` showed `SingleEmail Max=15 Remaining=-1`, and the verification flow logged `The org can send a maximum of 15 single emails per day to external email addresses`. OTP generation still worked, but Salesforce blocked external email delivery until GMT reset.
- The active Customer Self Service agent runtime user is `customer_self_service_agent@00dg5000005qpun1460074599.ext` (`Einstein Agent User`). Trace this user, not only the admin user, when debugging live Agentforce action execution.
- Temporary manual-testing mode was active on 2026-05-07 only. On 2026-05-08, deploy `0Afg5000007gZgZCAU` restored verification-first OTP metadata. A traced preview run logged `WF_EMAIL_SENT` to the clean Contact email, resolved `customerType=Contact`, and accepted the generated code for account summary. Tester mailbox confirmation remains the final manual check.
- Manual Track A UAT was completed on 2026-05-07 through Builder/Preview prompts. Account summary, service request creation (`00001029`), escalation, case-number safety, and attachment boundary all matched expected user-visible behavior. A follow-up planner-bundle routing cue deploy improved the Spanish prompt so it now behaves like Issue Reporting in preview; a Testing Center topic-selection assertion is optional confirmation only.
- Customer Self Service has an active Einstein Agent runtime user: `customer_self_service_agent@00dg5000005qpun1460074599.ext`. Scheduling Agent also has an active runtime user, but local `Scheduling_Agent` metadata does not include the built-in OTP verification topic. Use `Agentforce_Service_Agent` as the closer built-in verification-template comparison.
- 2026-05-08 mailbox debugging: `SingleEmail` dropped to `Remaining=8` and runtime logs showed `WF_EMAIL_SENT`, so Salesforce is queuing OTP emails. One attempt resolved to Contact `003g500000FXl7ZAAT` / `keshavchaudhary131@gmail.com`; another resolved to User `005g5000006PiUlAAK` / `keshav.chaudhary@hytectpro.com`. If inbox receipt fails after `WF_EMAIL_SENT`, check Email Log Files/spam/quarantine and identity collisions, not Agentforce permissions.
- `AI_API_Health_Bridge` is a temporary published Phase 1 topic for runtime proof only. When later phases deliver permanent production capabilities, remove that topic and its planner-local action from the customer-facing planner bundle, but keep the underlying health endpoint, Apex bridge, tests, and ops runbooks until replacement monitoring exists.

# Project Memory

- This repo is the Salesforce Agentforce workspace unless the user explicitly asks to scaffold external platform code here.
- The NestJS/Railway/OpenAI/LangChain/Pinecone/Open WebUI plan.
- First milestone: Agentforce -> Apex -> Named Credential -> Railway NestJS -> structured response -> Agentforce.
- Open WebUI is production internal chat. It calls the NestJS OpenAI-compatible gateway, not OpenAI directly.
- React chat is customer-facing and must use customer-safe policy, identity/session rules, rate limits, and escalation guardrails.
- React chat may be embedded in Experience Cloud or another Salesforce-hosted page shell, but the React codebase and backend lifecycle remain in the external platform.
- Agent services call `ModelRouter`; `ModelRouter` calls provider adapters.
- OpenAI is production v1. Anthropic, Azure OpenAI, Gemini, and OpenAI-compatible self-hosted providers remain extension paths.
- Pinecone is production v1 vector DB. Keep vector DB access behind an interface.
- Agentforce behavior changes need eval prompts or Testing Center cases.
- Production go-live requires UAT, security review, release approval, observability, rollback, and readiness gates.
- Built-in `Service Customer Verification` resolves a matching Salesforce `User` before a matching `Contact` when both share the same email. Use a non-colliding `Contact` email for customer self-service OTP testing unless login-user verification is intentional.
- OTP operational guidance for this org: tell customers to check spam or junk, wait about 1 to 2 minutes, resend only when needed, and use only the newest code because resends invalidate older OTPs.
- Earlier direct Salesforce mail tests and verification-flow logs proved the Agentforce OTP path can queue mail successfully. Current missing OTPs must also be checked against org email limits before assuming mailbox filtering.
- Manual OTP sends can appear to contradict Agentforce sends if they happened earlier or used a different recipient/execution user. On 2026-05-07, manual Flow send at 06:40 UTC succeeded for `keshavchaudhary131@gmail.com`; later Agentforce runtime send at 08:03 UTC failed for the plus-alias Contact because `SingleEmail` was exhausted.
- On 2026-05-07, `CustomerSelfServiceEscalateRequest` was hardened so escalation by customer-provided case number requires the verified account context. Targeted deploy `0Afg5000007dTW0CAM` passed 8/8 `CustomerSelfServiceActionsTest` tests.
- `Work.com Only User` licenses cannot receive the `Customer_Self_Service_Agent` permission set because that set requires Account, Contact, and Case access.
- Dev-only fallback: the latest OTP can be read from the admin debug log near the `GenerateVerificationCode` flow action for `SvcCopilotTmpl__SendVerificationCode`. Do not treat this as a production support pattern.
- Active `GenAiPlannerBundle` deployment requires the supported lifecycle sequence: `sf agent deactivate --api-name Customer_Self_Service_Agent --target-org AgentForce`, deploy, then `sf agent activate --api-name Customer_Self_Service_Agent --target-org AgentForce`, followed by `sf agent preview ...` validation.
- On 2026-05-07, the org hit the Developer Edition external `SingleEmail` limit: `/limits` showed `SingleEmail Max=15 Remaining=-1`, and the verification flow logged `The org can send a maximum of 15 single emails per day to external email addresses`. OTP generation still worked, but Salesforce blocked external email delivery until GMT reset.
- The active Customer Self Service agent runtime user is `customer_self_service_agent@00dg5000005qpun1460074599.ext` (`Einstein Agent User`). Trace this user, not only the admin user, when debugging live Agentforce action execution.
- Temporary manual-testing mode is active as of 2026-05-07: the planner bundle no longer routes to OTP verification first and instead collects account/contact identifiers directly from the tester. Live preview validation succeeded for direct account summary by email, case creation (`00001028`), and escalation without email verification.
- Manual Track A UAT was completed on 2026-05-07 through Builder/Preview prompts. Account summary, service request creation (`00001029`), escalation, case-number safety, and attachment boundary all matched expected user-visible behavior. A follow-up planner-bundle routing cue deploy improved the Spanish prompt so it now behaves like Issue Reporting in preview; a Testing Center topic-selection assertion is optional confirmation only.
- Re-enable production-style OTP testing by restoring the verification-first planner bundle/genAiFunction metadata and redeploying with `sf agent deactivate`, `sf project deploy start`, and `sf agent activate`.

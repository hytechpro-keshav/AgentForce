<?xml version="1.0" encoding="UTF-8" ?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <customSettingsType>Hierarchy</customSettingsType>
    <description
  >Protected hierarchy custom setting holding the OTP secret pepper, from-address, and tunables (TTL, attempt cap, rate limit). The org-default record is populated post-deploy and is NOT source-controlled so no secret is committed. OtpService falls back to safe constants when unset.</description>
    <enableFeeds>false</enableFeeds>
    <label>OTP Config</label>
    <visibility>Protected</visibility>
</CustomObject>
